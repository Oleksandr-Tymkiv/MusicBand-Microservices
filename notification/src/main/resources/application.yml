spring:
  application:
    name: "notification"
  config:
    import: "optional:configserver:http://localhost:8071/"
  data:
    redis:
      connect-timeout: 2s
      host: localhost
      port: 6379
      timeout: 1s
  cloud:
    function:
      definition: tourUpdatedMessage;tourDeletedMessage;ticketUpdatedMessage
    stream:
      bindings:
        tourUpdatedMessage-in-0:
          destination: notification-tour-updated-events
        tourDeletedMessage-in-0:
          destination: notification-tour-deleted-events
        ticketUpdatedMessage-in-0:
          destination: notification-new-tickets-events
      kafka:
        binder:
          brokers:
            - localhost:9092
  mail:
    host: smtp.gmail.com
    port: 587
    username: "${email_smtp}"
    password: "${smtp_password}"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8073
logging:
  level:
    com:
      musicband:
        notification: DEBUG